<!DOCTYPE html>
<html>

<%= render 'layouts/head' %>

<body>

    <%= render 'layouts/header' %>
    <%= render 'shared/modal' %>
    <%# アラート機能 #%>
    <% flash.each do  |message_type, message| %>
    <div class="uk-alert uk-alert-<%= message_type %>"><%= message %></div>
    <% end %>


    <%= yield %>

    <!--<%= render 'layouts/footer' %>-->
</body>

</html>
