<% if user_signed_in? %>
<%= form_with(model: [post, comment], url: post_comments_path(@post) ) do |f| %>
<%= f.text_area :comment, class: "uk-textarea", placeholder: "コメントを入力してください", rows: 3 %>
<%= f.submit "送信", class: "uk-button uk-button-default uk-width-1-1" %>
<% end %>
<% end %>
